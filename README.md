# CF Sub Converter

基於 Cloudflare Workers 的輕量級訂閱轉換工具。無需伺服器，利用 Cloudflare 強大的邊緣網絡進行高速轉換。

[![Deploy to Cloudflare Workers](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/sammy0101/cf-sub-converter)

## 🌟 特性

*   **Serverless 部署**：完全運行在 Cloudflare Workers 上，無需維護 VPS。
*   **隱私安全**：代碼開源，轉換過程在邊緣節點完成，不記錄用戶日誌。
*   **高性能**：依託 Cloudflare 全球網絡，延遲極低。
*   **多協議支持**：支持 **Clash**, **V2Ray**, **Sing-box** 等主流格式轉換。

## 🚦 預設分流策略

本轉換器預設包含以下精細化的分流規則組，滿足日常需求：

*   💬 **香港AI 服務** (針對 ChatGPT, Claude 等 AI 服務優化)
*   🌐 **非中國** (國外流量)
*   🔒 **國內服務** (中國大陸流量，直連)
*   🏠 **私有網絡** (局域網訪問)
*   🐟 **漏網之魚** (其他未匹配流量)
*   🛑 **廣告攔截** (各類廣告域名屏蔽)

## 🚀 部署教程 (Deployment)

為了確保部署按鈕能正確識別配置，請務必按照以下順序操作：

### 第一步：Fork 本項目

點擊右上角的 **[Fork]** 按鈕，將本倉庫複製到你自己的 GitHub 帳號下。
*(這是為了確保 `wrangler.toml` 配置文件能被 Cloudflare 正確讀取，從而自動彈出變量設置框)*

### 第二步：一鍵部署

回到你 Fork 後的倉庫頁面，點擊上方的 **Deploy to Cloudflare Workers** 按鈕。

### 第三步：配置環境變量 (Secrets)

在 Cloudflare 的部署引導頁面中，系統會自動詢問以下 Secrets，請填入你的相關信息：

| 變量名 (Secret) | 說明 | 獲取方式 |
| :--- | :--- | :--- |
| `CF_ACCOUNT_ID` | Cloudflare 帳戶 ID | CF 控制台首頁右側可見 |
| `CF_API_TOKEN` | Cloudflare API 令牌 | 用於 Workers 操作權限 |
| `CF_KV_ID` | KV Namespace ID | 用於緩存或存儲配置 |

> ⚠️ **注意**：如果不填寫這些變量，轉換器可能無法正常使用高級功能或緩存功能。

## 📖 使用方法

部署成功後，你會獲得一個類似 `https://your-worker.your-name.workers.dev` 的網址。

**基礎 URL 格式：**
```
https://<你的域名>/sub?target=<目標客戶端>&url=<原始訂閱鏈接>
```

**參數說明：**

*   `target`: 目標客戶端類型，例如 `clash`, `sing-box`, `v2ray`
*   `url`: 你的原始訂閱鏈接 (建議進行 URL Encode)
*   `config`: (可選) 自定義遠程規則地址

**示例：**
```
https://cf-sub-converter.sammy.workers.dev/sub?target=clash&url=https%3A%2F%2Fexample.com%2Fapi%2Fv1%2Fsubscribe
```

## 🛠️ 本地開發

```bash
# 克隆倉庫
git clone https://github.com/sammy0101/cf-sub-converter.git
cd cf-sub-converter

# 安裝依賴
npm install

# 本地調試
npm run dev
```

## ⚠️ 免責聲明

*   本項目僅供學習和技術研究使用。
*   請勿將本項目用於任何非法用途。
*   作者不對使用本項目產生的任何後果負責。

---
